# AK’Games — Refonte éditoriale définitive

## Contenu du pack

- 40 fichiers JSON corrigés et enrichis
- 8 754 entrées jouables
- intégration consolidée des packs V2, V3, V4 et V5
- priorité systématique donnée à la version la plus récente de chaque base

## Contrôles réalisés

- tous les fichiers JSON sont lisibles
- aucun identifiant dupliqué dans une même base
- aucun contenu jouable strictement identique entre deux jeux
- aucun chevauchement restant entre les scènes de `Mime` et `Imitation`
- formats racine et volumes contrôlés
- archive ZIP testée après création

## Dernières corrections croisées

- retrait des scènes d’`Imitation` qui reprenaient directement celles de `Mime`
- ajout de 100 nouvelles imitations conçues pour la voix, les accents, le rythme et les bruitages
- remplacement des deux questions identiques partagées par `Bombe` et `Même cerveau`

Le fichier `CONTROLE_FINAL.json` contient le détail automatisé du contrôle.
